package com.yong2ss.toberich.domain.draw;

import com.yong2ss.toberich.dto.draw.CustomDrawDto;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.Getter;
import lombok.Setter;

import javax.persistence.*;

/*@Entity
@NamedNativeQuery(
        name = "selectAllLotto",
        query = "SELECT M.DRAW1 AS lottoNum, COUNT(*) AS cnt FROM lotto as M GROUP BY M.DRAW1",
        resultSetMapping = "lottoAllResult" )
@SqlResultSetMapping(
        name = "lottoAllResult",
        classes = @ConstructorResult(
            targetClass = CustomDrawDto.class,
            columns = {
                    @ColumnResult(name="lottoNum", type = Integer.class),
                    @ColumnResult(name="cnt", type = Integer.class)
            }
        )
)
public class DrawStatis {
}*/


/*
    private int lottoNum;

    private int cnt;
*/

